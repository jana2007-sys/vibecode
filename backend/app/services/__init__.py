"""Services package — application/business layer (skeletons).

Each service has a single responsibility and receives its collaborators through
the constructor (dependency injection). Method bodies are intentionally stubbed.
"""
